# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: i18n/i18n.spec.ts >> i18n >> /fa/ has dir="rtl" on html element
- Location: tests/i18n/i18n.spec.ts:4:3

# Error details

```
Error: expect(locator).toHaveAttribute(expected) failed

Locator:  locator('html')
Expected: "rtl"
Received: ""

Call log:
  - Expect "toHaveAttribute" with timeout 5000ms
  - waiting for locator('html')
    13 × locator resolved to <html lang="en">…</html>
       - unexpected value "null"

```

```yaml
- document:
  - main
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('i18n', () => {
  4  |   test('/fa/ has dir="rtl" on html element', async ({ page }) => {
  5  |     await page.goto('/fa/');
  6  |     const html = page.locator('html');
> 7  |     await expect(html).toHaveAttribute('dir', 'rtl');
     |                        ^ Error: expect(locator).toHaveAttribute(expected) failed
  8  |   });
  9  | 
  10 |   test('/en/ has dir="ltr" on html element', async ({ page }) => {
  11 |     await page.goto('/en/');
  12 |     const html = page.locator('html');
  13 |     await expect(html).toHaveAttribute('dir', 'ltr');
  14 |   });
  15 | 
  16 |   test('Clicking language switch from /en/ navigates to /fa/ equivalent', async ({ page }) => {
  17 |     await page.goto('/en/tools');
  18 |     const langSwitch = page.locator('.lang-switch');
  19 | 
  20 |     const href = await langSwitch.getAttribute('href');
  21 |     expect(href).toContain('/fa/');
  22 | 
  23 |     await langSwitch.click();
  24 |     await expect(page).toHaveURL(/\/fa\/tools/);
  25 |   });
  26 | 
  27 |   test('/fa/ shows Persian text in navigation', async ({ page }) => {
  28 |     await page.goto('/fa/');
  29 |     const nav = page.locator('#main-nav');
  30 | 
  31 |     await expect(nav).toContainText('خانه');
  32 |     await expect(nav).toContainText('ابزارها');
  33 |     await expect(nav).toContainText('دسته‌بندی‌ها');
  34 |     await expect(nav).toContainText('مشارکت');
  35 |   });
  36 | });
  37 | 
```