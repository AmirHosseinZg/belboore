# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: core/smoke.spec.ts >> Core site smoke tests >> Header >> Theme toggle button exists
- Location: tests/core/smoke.spec.ts:51:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('#theme-toggle')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('#theme-toggle')

```

```yaml
- main:
  - img
  - 'heading "404: Not Found (trailingSlash is set to \"never\")" [level=1]'
  - text: "Path: /en/"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Core site smoke tests', () => {
  4  |   test('Homepage loads in English at /en/', async ({ page }) => {
  5  |     const response = await page.goto('/en/');
  6  |     expect(response?.status()).toBe(200);
  7  |   });
  8  | 
  9  |   test('Homepage loads in Persian at /fa/', async ({ page }) => {
  10 |     const response = await page.goto('/fa/');
  11 |     expect(response?.status()).toBe(200);
  12 |   });
  13 | 
  14 |   test('Root / redirects to /fa/', async ({ page }) => {
  15 |     await page.goto('/');
  16 |     expect(page.url()).toContain('/fa/');
  17 |   });
  18 | 
  19 |   test('Title contains "Belboore"', async ({ page }) => {
  20 |     await page.goto('/en/');
  21 |     await expect(page).toHaveTitle(/Belboore/);
  22 |   });
  23 | 
  24 |   test.describe('Header', () => {
  25 |     test('Main navigation links are visible (desktop)', async ({ page }) => {
  26 |       await page.setViewportSize({ width: 1280, height: 800 });
  27 |       await page.goto('/en/');
  28 | 
  29 |       const nav = page.locator('#main-nav');
  30 |       await expect(nav).toBeVisible();
  31 | 
  32 |       const navLinks = nav.locator('.header__nav-link');
  33 |       const labels = await navLinks.allTextContents();
  34 |       expect(labels.some(l => l.includes('Home'))).toBe(true);
  35 |       expect(labels.some(l => l.includes('Tools'))).toBe(true);
  36 |       expect(labels.some(l => l.includes('Categories'))).toBe(true);
  37 |     });
  38 | 
  39 |     test('Header logo links to homepage', async ({ page }) => {
  40 |       await page.goto('/en/tools');
  41 |       const logo = page.locator('.header__logo');
  42 |       await expect(logo).toBeVisible();
  43 | 
  44 |       const logoHref = await logo.getAttribute('href');
  45 |       expect(logoHref).toBe('/en/');
  46 | 
  47 |       await logo.click();
  48 |       await expect(page).toHaveURL('/en/');
  49 |     });
  50 | 
  51 |     test('Theme toggle button exists', async ({ page }) => {
  52 |       await page.goto('/en/');
  53 |       const toggle = page.locator('#theme-toggle');
> 54 |       await expect(toggle).toBeVisible();
     |                            ^ Error: expect(locator).toBeVisible() failed
  55 |     });
  56 | 
  57 |     test('Language switcher exists and has correct text', async ({ page }) => {
  58 |       await page.goto('/en/');
  59 |       const langSwitch = page.locator('.lang-switch');
  60 |       await expect(langSwitch).toBeVisible();
  61 | 
  62 |       await expect(langSwitch).toContainText('فارسی');
  63 |     });
  64 |   });
  65 | 
  66 |   test('Footer is visible with copyright', async ({ page }) => {
  67 |     await page.goto('/en/');
  68 |     const footer = page.locator('.footer');
  69 |     await expect(footer).toBeVisible();
  70 | 
  71 |     const copyright = footer.locator('.footer__copyright');
  72 |     await expect(copyright).toContainText('Belboore');
  73 |   });
  74 | 
  75 |   test('404 page shows for /en/nonexistent-page', async ({ page }) => {
  76 |     const response = await page.goto('/en/nonexistent-page');
  77 |     expect(response?.status()).toBe(404);
  78 | 
  79 |     await expect(page.locator('.not-found__code')).toContainText('404');
  80 |   });
  81 | });
  82 | 
```