---
import ToolPrimitives from './ToolPrimitives.astro';
import ToolUtils from './ToolUtils.astro';
import ToolWorkspace from './ToolWorkspace.astro';
---

<ToolPrimitives />
<ToolUtils />

<ToolWorkspace title="Lorem Ipsum Generator">
  <div class="tool-toolbar">
    <div class="tool-field" style="flex: 0 0 auto;">
      <label class="tool-field__label" for="li-type">Type</label>
      <select id="li-type" class="tool-select">
        <option value="paragraphs">Paragraphs</option>
        <option value="sentences">Sentences</option>
        <option value="words">Words</option>
      </select>
    </div>
    <div class="tool-field" style="flex: 0 0 auto;">
      <label class="tool-field__label" for="li-amount">Amount</label>
      <input id="li-amount" class="tool-input" type="number" min="1" max="20" value="3" style="width: 80px;" />
    </div>
    <button type="button" id="li-generate" class="tool-btn tool-btn--primary" style="align-self: flex-end;">Generate</button>
  </div>
  <div class="tool-toolbar">
    <button type="button" id="li-copy" class="tool-btn">Copy</button>
    <button type="button" id="li-clear" class="tool-btn">Clear</button>
  </div>
  <div class="tool-field">
    <label class="tool-field__label" for="li-output">Output</label>
    <div id="li-output" class="tool-output tool-output--empty" role="status" aria-live="polite">Click Generate to create placeholder text</div>
  </div>
  <div class="tool-note">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
    All processing happens in your browser. Your data is never uploaded.
  </div>
</ToolWorkspace>

<script>
  const liOutput = document.getElementById('li-output')!;
  const liType = document.getElementById('li-type') as HTMLSelectElement;
  const liAmount = document.getElementById('li-amount') as HTMLInputElement;

  function showOutput(text: string) {
    window.__belbooreTools.showOutput(liOutput, text);
  }
  function showEmpty(text: string) {
    window.__belbooreTools.showEmpty(liOutput, text);
  }

  const loremSentences = [
    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
    "Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
    "Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris.",
    "Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore.",
    "Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia.",
    "Deserunt mollit anim id est laborum sed ut perspiciatis unde omnis iste natus.",
    "Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit.",
    "Neque porro quisquam est, qui dolorem ipsum quia dolor sit amet consectetur.",
    "Ut perspiciatis unde omnis iste natus error sit voluptatem accusantium.",
    "Totam rem aperiam eaque ipsa quae ab illo inventore veritatis et quasi.",
    "Architecto beatae vitae dicta sunt explicabo nemo enim ipsam voluptatem.",
    "Quis autem vel eum iure reprehenderit qui in ea voluptate velit esse.",
    "At vero eos et accusamus et iusto odio dignissimos ducimus qui blanditiis.",
    "Et harum quidem rerum facilis est et expedita distinctio nam libero tempore.",
    "Cum soluta nobis est eligendi optio cumque nihil impedit quo minus id.",
    "Temporibus autem quibusdam et aut officiis debitis aut rerum necessitatibus.",
    "Itaque earum rerum hic tenetur a sapiente delectus ut aut reiciendis.",
    "Voluptatibus maiores alias consequatur aut perferendis doloribus asperiores.",
    "Nam libero tempore cum soluta nobis est eligendi optio cumque nihil.",
    "Facilis est et expedita distinctio nam libero tempore cum soluta nobis.",
    "Omnis voluptas assumenda est omnis dolor repellendus temporibus autem.",
    "Quibusdam et aut officiis debitis aut rerum necessitatibus saepe eveniet.",
    "Itaque earum rerum hic tenetur a sapiente delectus ut aut reiciendis.",
    "Voluptatibus maiores alias consequatur aut perferendis doloribus asperiores.",
  ];

  function shuffle(arr: string[]): string[] {
    const a = [...arr];
    for (let i = a.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
  }

  function pickSentences(count: number): string[] {
    const pool = [...loremSentences];
    const result: string[] = [];
    while (result.length < count) {
      if (pool.length === 0) pool.push(...shuffle(loremSentences));
      result.push(pool.pop()!);
    }
    return result;
  }

  function generate() {
    const type = liType.value;
    let amount = parseInt(liAmount.value, 10);
    if (isNaN(amount) || amount < 1) amount = 1;
    if (amount > 20) amount = 20;

    if (type === 'words') {
      const words = pickSentences(Math.ceil(amount / 8)).join(' ').split(/\s+/).slice(0, amount);
      let text = words.join(' ');
      if (!/[.!?]$/.test(text)) text += '.';
      showOutput(text);
    } else if (type === 'sentences') {
      showOutput(pickSentences(amount).join(' '));
    } else {
      const paragraphs: string[] = [];
      for (let i = 0; i < amount; i++) {
        paragraphs.push(pickSentences(3 + Math.floor(Math.random() * 5)).join(' '));
      }
      showOutput(paragraphs.join('\n\n'));
    }
  }

  document.getElementById('li-generate')!.addEventListener('click', generate);

  document.getElementById('li-copy')!.addEventListener('click', () => {
    const text = liOutput.textContent || '';
    if (!text || liOutput.classList.contains('tool-output--empty')) return;
    window.__belbooreTools.copyToClipboard(text, document.getElementById('li-copy')!, 1500);
  });

  document.getElementById('li-clear')!.addEventListener('click', () => {
    liAmount.value = '3';
    liType.value = 'paragraphs';
    showEmpty('Click Generate to create placeholder text');
  });
</script>
